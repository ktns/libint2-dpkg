libint (2.0.3-1~1.gbp09cb11) UNRELEASED; urgency=low

  ** SNAPSHOT build @09cb11597e1c693b24475462c797528fe6acf2e7 **

  * dh_make
  * configure with --enable-shared
  * make with SHELL=/bin/sh
  * Build test program with -Wl,-rpath
  * make install with SHELL=/bin/bash
  * Add build dependency on bash package
  * Enable parallel build
  * No pkgconfig files
  * Skip tests
  * Package information
  * correct lintian errors
  * dh_make
  * configure with --enable-shared
  * make with SHELL=/bin/sh
  * Build test program with -Wl,-rpath
  * make install with SHELL=/bin/bash
  * Add build dependency on bash package
  * Enable parallel build
  * No pkgconfig files
  * Skip tests
  * Package information
  * Correct lintian errors
  * dh_make
  * configure with --enable-shared
  * make with SHELL=/bin/sh
  * Build test program with -Wl,-rpath
  * make install with SHELL=/bin/bash
  * Add build dependency on bash package
  * Enable parallel build
  * No pkgconfig files
  * Skip tests
  * Package information
  * Correct lintian errors
  * Snapshot changelog
  * Clean source tree manually
  * Copyright notation

 -- Katsuhiko Nishimra <ktns.87@gmail.com>  Sat, 30 Nov 2013 21:13:21 +0900
