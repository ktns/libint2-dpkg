libint (2.0.3-1~4.gbp75aec5) UNRELEASED; urgency=low

  ** SNAPSHOT build @75aec5f78e9a86426bd5bfe60239698afe955e75 **

  * dh_make
  * configure with --enable-shared
  * make with SHELL=/bin/sh
  * Build test program with -Wl,-rpath
  * make install with SHELL=/bin/bash
  * Add build dependency on bash package
  * Enable parallel build
  * No pkgconfig files
  * Skip tests
  * Package information
  * correct lintian errors
  * dh_make
  * configure with --enable-shared
  * make with SHELL=/bin/sh
  * Build test program with -Wl,-rpath
  * make install with SHELL=/bin/bash
  * Add build dependency on bash package
  * Enable parallel build
  * No pkgconfig files
  * Skip tests
  * Package information
  * Correct lintian errors
  * dh_make
  * configure with --enable-shared
  * make with SHELL=/bin/sh
  * Build test program with -Wl,-rpath
  * make install with SHELL=/bin/bash
  * Add build dependency on bash package
  * Enable parallel build
  * No pkgconfig files
  * Skip tests
  * Package information
  * Correct lintian errors
  * Clean source tree manually
  * Copyright notation
  * Delete a comment from dh_make
  * Correct names of dirs and install files
  * Remove debian README files
  * Use dpkg-buildflags explicitly

 -- Katsuhiko Nishimra <ktns.87@gmail.com>  Sun, 01 Dec 2013 01:16:20 +0900
